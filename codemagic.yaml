workflows:
  android-debug:
    name: Voice Recorder Debug APK
    environment:
      java: 17
    scripts:
      - chmod +x ./gradlew
      - ./gradlew clean assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/*.apk
