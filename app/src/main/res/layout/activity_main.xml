<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">

    <TextView
        android:id="@+id/tvTimer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="00:00"
        android:textSize="48sp"
        android:textStyle="bold"/>

    <com.example.voicerecorder.WaveformView
        android:id="@+id/waveformView"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="20dp"
        android:background="#1E1E1E"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="30dp">

        <Button android:id="@+id/btnRecord" android:text="🎤 Record" android:layout_margin="4dp"/>
        <Button android:id="@+id/btnPause" android:text="⏸ Pause" android:layout_margin="4dp"/>
        <Button android:id="@+id/btnResume" android:text="▶ Resume" android:layout_margin="4dp"/>
        <Button android:id="@+id/btnStop" android:text="⏹ Stop" android:layout_margin="4dp"/>
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="10dp">

        <Button android:id="@+id/btnPlay" android:text="▶ Play" android:layout_margin="4dp"/>
        <Button android:id="@+id/btnShare" android:text="📤 Share" android:layout_margin="4dp"/>
        <Button android:id="@+id/btnDelete" android:text="🗑 Delete" android:layout_margin="4dp"/>
    </LinearLayout>

</LinearLayout>
